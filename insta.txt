==> Authentication
  => register
  => login
  => logout (token blacklisting) // mostly asked in intervews
  => [OTP bases registration]

==> post
  => create
  => can see the feed
  => like posts (collections)
  => save posts

==> users
  => followers
  =>following


  user = {
    username:String,
    email:String,
    password:String,
    bio:String,
    profile_image:String
  }